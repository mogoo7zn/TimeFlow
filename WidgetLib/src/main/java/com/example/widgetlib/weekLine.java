package com.example.widgetlib;

import android.content.Context;
import android.util.AttributeSet;
import android.view.View;

import androidx.annotation.Nullable;

public class weekLine extends View {
    public weekLine(Context context) {
        super(context);
    }

    public weekLine(Context context, @Nullable AttributeSet attrs) {
        super(context, attrs);
    }

    public weekLine(Context context, @Nullable AttributeSet attrs, int defStyleAttr) {
        super(context, attrs, defStyleAttr);
    }

    public weekLine(Context context, @Nullable AttributeSet attrs, int defStyleAttr, int defStyleRes) {
        super(context, attrs, defStyleAttr, defStyleRes);
    }
}
