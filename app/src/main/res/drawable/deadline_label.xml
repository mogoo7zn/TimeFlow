<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 这里是设置为四周 也可以单独设置某个位置为圆角-->
    <corners android:radius="10dp"/>
    <padding
        android:bottom="@dimen/time_label_padding_vertical"
        android:left="@dimen/time_label_padding_horizontal"
        android:right="@dimen/time_label_padding_horizontal"
        android:top="@dimen/time_label_padding_vertical" />
    <solid android:color="@color/white" />
    <stroke android:width="1dp"
        android:color="@color/deadline_label" />
    <!--虚线: android:dashGap="5dp", 虚线宽度: android:dashWidth="3dp"-->
</shape>