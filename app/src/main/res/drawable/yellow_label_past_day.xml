<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto" >
    <!-- 这里是设置为四周 也可以单独设置某个位置为圆角-->
    <corners android:topRightRadius="5dp"
        android:bottomRightRadius="5dp"/>
    <padding
        android:left="@dimen/small_label_padding_horizontal"
        android:right="@dimen/small_label_padding_horizontal"
        android:top="@dimen/days_label_padding"
        android:bottom="@dimen/days_label_padding">
    </padding>
    <solid android:color="@color/yellow_label_bg_deep" />
    <stroke android:width="1dp"
        android:color="@color/yellow_label_line" />
    <!--虚线: android:dashGap="5dp", 虚线宽度: android:dashWidth="3dp"-->
</shape>